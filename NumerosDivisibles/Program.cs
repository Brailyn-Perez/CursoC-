using NumerosDivisibles;

internal class Program
{
    private static void Main(string[] args)
    {
        //2) Crear un programa que cuente cuántos números, de un conjunto de 10, son divisibles por 3 o por 5.
        VerificarDivisibles verificarDivisibles = new VerificarDivisibles();
        verificarDivisibles.verificarDivisibles();

    }
}